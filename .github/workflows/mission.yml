name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Debug Run
        run: |
          ./nuclei -version
          # Use -debug to see the actual HTTP exchange
          ./nuclei -target https://pay.wallet.tg -debug
