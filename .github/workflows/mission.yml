name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Scan
        run: |
          ./nuclei -target https://pay.wallet.tg -severity critical -silent
