name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Nuclei Scan
        uses: projectdiscovery/nuclei-action@v1
        with:
          target: https://pay.wallet.tg
          severity: critical
