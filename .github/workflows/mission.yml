name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Verbose Scan
        run: |
          ./nuclei -target https://pay.wallet.tg -severity critical -vv
