name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Fast Scan
        run: |
          ./nuclei -target https://pay.wallet.tg -severity critical -timeout 5 -rate-limit 50
