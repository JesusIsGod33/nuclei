name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Run Nuclei Scan
        run: /path/to/your/nuclei -target https://pay.wallet.tg -severity critical
