name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Fast Scan
        run: |
          # Use fewer templates to ensure a fast, confirmed result
          ./nuclei -target https://pay.wallet.tg -severity critical,high -stats -timeout 10
