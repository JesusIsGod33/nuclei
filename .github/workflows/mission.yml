name: TITAN_MISSION
on: [workflow_dispatch]
jobs:
  run:
    runs-on: self-hosted
    steps:
      - name: Run Nuclei Scan
        run: /home/infuselink/tools/runner/_work/nuclei/nuclei -target https://pay.wallet.tg -severity critical
